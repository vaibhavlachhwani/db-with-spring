create table course(
                       id bigint not null,
                       name varchar(60) not null,
                       author varchar(60) not null,
                       primary key (id)
);